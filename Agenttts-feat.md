نظام الوكلاء على شحنك شات الإصدار 1

المطلوب: نظام وكلاء داخل متجر Laravel
الفكرة العامة
إضافة نظام وكلاء داخل المتجر.
الوكيل يستطيع بيع خدمات داخل المتجر بطريقتين:
.1 يضيف خدماته الخاصة.
.2 يعيد بيع خدمات موجودة في المتجر مع إضافة ربح خاص به.
كل عملية دفع تتم داخل المتجر، وليس عند الوكيل.
المتجر يستلم كامل المبلغ، ثم يتم حساب ربح الوكيل داخل النظام.

1. طلب الانضمام كوكيل
   يجب إنشاء صفحة اسمها مثلاً:
   /agent-register
   أو:
   /become-agent
   من خلالها يرسل المستخدم طلب وكالة.
   بيانات طلب الوكالة
   ⦁ الاسم
   ⦁ البريد
   ⦁ رقم الهاتف
   ⦁ اسم المتجر / اسم الوكيل
   ⦁ اسم الرابط المطلوب
   ⦁ وصف مختصر عن خدماته
   ⦁ خبرته
   ⦁ طريقة استلام الأرباح
   ⦁ ملاحظات إضافية
   حالة الطلب
   طلب الوكالة يكون له حالات:
   ⦁ قيد المراجعة
   ⦁ مقبول
   ⦁ مرفوض
   من يقبل الطلب؟
   الأدمن من لوحة التحكم.
   في لوحة الأدمن يجب وجود صفحة:
   طلبات الوكالة
   الأدمن يستطيع:
   ⦁ مشاهدة الطلبات
   ⦁ قبول الطلب
   ⦁ رفض الطلب مع سبب
   ⦁ تحويل المستخدم إلى وكيل بعد القبول

2. رابط خاص لكل وكيل
   يفضل أن يكون لكل وكيل رابط فرعي خاص به.
   مثال:
   https://username.s7sh.com
   أو إذا كان أسهل مبدئياً:
   https://s7sh.com/agent/username
   المطلوب
   كل وكيل يكون له username خاص وغير مكرر.
   مثال:
   ⦁ ahmad.s7sh.com
   ⦁ mohammad.s7sh.com
   أو:
   ⦁ s7sh.com/agent/ahmad
   ⦁ s7sh.com/agent/mohammad
   ملاحظة مهمة للمبرمج
   الرابط الفرعي subdomain أفضل كشكل احترافي، لكنه يحتاج إعدادات على السيرفر و DNS.
   إذا كان تنفيذه يأخذ وقت، نبدأ برابط:
   /agent/username
   ثم نطوره لاحقاً إلى subdomain.

3. صفحة الوكيل العامة
   كل وكيل له صفحة خاصة تعرض:
   ⦁ اسم الوكيل
   ⦁ صورة أو شعار
   ⦁ وصف مختصر
   ⦁ متوسط التقييم
   ⦁ عدد الطلبات المكتملة
   ⦁ الخدمات الخاصة به
   ⦁ الخدمات التي يعيد بيعها
   ⦁ تقييمات العملاء
   عرض الخدمة في صفحة الوكيل
   كل خدمة تظهر فيها:
   ⦁ صورة الخدمة
   ⦁ عنوان الخدمة
   ⦁ السعر
   ⦁ مدة التنفيذ
   ⦁ تقييم الخدمة
   ⦁ عدد التقييمات
   ⦁ زر الطلب

4. لوحة تحكم الوكيل
   بعد قبول الوكيل، تظهر له لوحة خاصة.
   تحتوي على:
   أ. بيانات الوكيل
   ⦁ اسم الوكيل
   ⦁ صورة / شعار
   ⦁ وصف
   ⦁ رابط صفحته
   ⦁ بيانات السحب
   ⦁ حالة الحساب

ب. إضافة خدمة خاصة
الوكيل يستطيع إضافة خدمة جديدة تحتوي على:
⦁ عنوان الخدمة
⦁ وصف الخدمة
⦁ صور
⦁ سعر
⦁ مدة التنفيذ
⦁ شروط الخدمة
لكن الخدمة لا تظهر مباشرة في الموقع.
حالات الخدمة
⦁ قيد المراجعة
⦁ مقبولة
⦁ مرفوضة
⦁ موقوفة
القاعدة المهمة
أي خدمة جديدة أو تعديل على خدمة موجودة يجب أن يذهب للأدمن للموافقة.

ج. إعادة بيع خدمات المتجر
الوكيل يستطيع اختيار خدمة موجودة في المتجر وإضافتها إلى صفحته مع ربح خاص به.
مثال:
⦁ سعر الخدمة الأصلية: 10$
⦁	ربح الوكيل: 3$
⦁ السعر النهائي للعميل: 13$
المطلوب برمجياً
يتم حفظ:
⦁ الخدمة الأصلية
⦁ الوكيل
⦁ ربح الوكيل
⦁ السعر النهائي
مهم جداً:
السعر النهائي يجب أن يحسب من السيرفر، وليس من المتصفح.

د. طلبات الوكيل
الوكيل يرى فقط الطلبات التي تخصه.
يعرض له:
⦁ رقم الطلب
⦁ اسم العميل
⦁ اسم الخدمة
⦁ السعر
⦁ حالة الدفع
⦁ حالة الطلب
⦁ تاريخ الطلب
⦁ زر تسليم الطلب
لا يستطيع رؤية طلبات وكلاء آخرين.

هـ. أرباح الوكيل
يجب وجود محفظة للوكيل.
المحفظة تحتوي على:
⦁ رصيد معلق
⦁ رصيد متاح للسحب
⦁ إجمالي الأرباح
⦁ إجمالي المسحوب
الفرق بين الرصيدين
الرصيد المعلق:
مبلغ الطلب قبل اكتماله.
الرصيد المتاح:
مبلغ يمكن للوكيل سحبه بعد اكتمال الطلب.

و. طلب السحب
الوكيل يستطيع طلب سحب أرباحه.
حقول طلب السحب:
⦁ المبلغ
⦁ طريقة الدفع
⦁ بيانات الدفع
⦁ ملاحظات
حالات السحب:
⦁ قيد المراجعة
⦁ مقبول
⦁ مرفوض
⦁ مدفوع
الأدمن فقط هو من يوافق على السحب.

5.  لوحة تحكم الأدمن
     يجب إضافة أقسام جديدة في لوحة الإدارة.
     أ. طلبات الوكالة
     الأدمن يستطيع:
     ⦁ مشاهدة طلبات الوكالة
     ⦁ قبول الطلب
     ⦁ رفض الطلب
     ⦁ كتابة سبب الرفض
     ⦁ تفعيل المستخدم كوكيل

     ب. إدارة الوكلاء
     الأدمن يستطيع:
     ⦁ مشاهدة كل الوكلاء
     ⦁ تفعيل وكيل
     ⦁ إيقاف وكيل
     ⦁ تعديل بياناته
     ⦁ مشاهدة خدماته
     ⦁ مشاهدة طلباته
     ⦁ مشاهدة أرباحه
     ⦁ منعه من إضافة خدمات
     ⦁ منعه من طلب السحب

     ج. مراجعة خدمات الوكلاء
     أي خدمة يضيفها الوكيل تظهر للأدمن.
     الأدمن يستطيع:
     ⦁ قبول الخدمة
     ⦁ رفض الخدمة
     ⦁ كتابة سبب الرفض
     ⦁ تحديد نسبة المتجر من الخدمة
     مثال:
     ⦁ سعر الخدمة: 100$
     ⦁	نسبة المتجر: 20%
     ⦁	ربح المتجر: 20$
     ⦁ ربح الوكيل: 80$

     د. إدارة السحوبات
     الأدمن يشاهد طلبات سحب الوكلاء.
     يستطيع:
     ⦁ قبول السحب
     ⦁ رفض السحب
     ⦁ تحديد أنه تم الدفع
     ⦁ كتابة ملاحظة

13. التفاصيل الفنية للتنفيذ
    أ. بنية URL
       ⦁ رابط الوكيل: /agent/{username}
       ⦁ صفحة التسجيل كوكيل: /become-agent
       ⦁ لوحة الوكيل: /agent/dashboard
       ⦁ إضافة خدمة: /agent/dashboard/services/create
       ⦁ طلبات الوكيل: /agent/dashboard/orders
       ⦁ المحفظة: /agent/dashboard/wallet

    ب. نظام الدفع
       ⦁ جميع المدفوعات تذهب إلى حساب المتجر الحالي
       ⦁ يتم حساب ربح الوكيل تلقائياً بعد كل دفع
       ⦁ الدعم لجميع طرق الدفع الحالية والمستقبلية التي يضيفها الأدمن
       ⦁ يتم ربط كل دفع بطلب محدد ووكيل معين
       ⦁ عملية حساب الربح تتم في الـ Backend فقط

    ج. نظام العمولات
       ⦁ يحدد الوكيل نسبة العمولة عند إنشاء/تعديل خدمته
       ⦁ العمولة تحسب من السعر الأساسي للخدمة
       ⦁ إذا كان سعر الخدمة: 100$ وعمولة الوكيل: 30%
           ⦁ ربح الوكيل: 30$
           ⦁ ربح المتجر: 70$
       ⦁ العمولة تختلف لكل خدمة حسب ما يحدده الوكيل

    د. التصميم المتجاوب
       ⦁ جميع الصفحات تدعم التصميم المتجاوب للجوال
       ⦁ لوحة تحكم الوكيل تعمل بشكل مثالي على جميع الأجهزة
       ⦁ واجهة المستخدم سلسة وسهلة الاستخدام على الشاشات الصغيرة

    هـ. الأداء
       ⦁ الترحيل (Pagination) لجميع القوائم الطويلة
       ⦁ قوائم الوكلاء الخدمات الطلبات السحوبات
       ⦁ تحسين سرعة التحميل للصفحات الرئيسية
       ⦁ تجنب تحميل البيانات غير الضرورية

    و. قاعدة البيانات
       الجداول المطلوبة:
       ⦁ agents: بيانات الوكلاء والحالة
       ⦁ agent_applications: طلبات الانضمام كوكيل
       ⦁ agent_services: الخدمات المخصصة للوكلاء
       ⦁ resold_services: الخدمات المعاد بيعها
       ⦁ agent_commissions: الأرباح المعلقة والمتاحة
       ⦁ withdrawal_requests: طلبات السحب
       ⦁ reviews: التقييمات والمراجعات
       ⦁ order_agent: ربط الطلبات بالوكلاء

    ز. سياسات الأمان (Laravel Policies)
       ⦁ AgentPolicy: تحكم في وصول الوكلاء لبياناتهم فقط
       ⦁ AdminPolicy: صلاحيات كاملة لوحة الإدارة
       ⦁ ServicePolicy: الوكيل لا يعدل إلا خدماته
       ⦁ FinancialPolicy: حماية البيانات المالية

    ح. الفلاتر والتحققات
       ⦁ فلاتر للوكلاء (حالة النشاط التاريخ إلخ)
       ⦁ فلاتر للخدمات (الحالة النوع السعر)
       ⦁ فلاتر للطلبات (الحالة التاريخ الوكيل)
       ⦁ تحقق من صحة البيانات المدخلة
       ⦁ تنظيف البيانات الخطيرة (XSS SQL Injection)

    ط. التنفيذ المرحلي (المحدد)
       المرحلة الأولى (الأساسيات):
       ⦁ نظام تسجيل الوكلاء ومراجعتهم
       ⦁ صفحات الوكلاء العامة
       ⦁ إدارة خدمات الوكلاء الأساسية
       ⦁ نظام الطلبات الأساسي
       ⦁ حساب الأرباح الأساسي
       ⦁ التحقق من صحة جميع العمليات

       المرحلة الثانية (التحسينات):
       ⦁ نظام إعادة بيع الخدمات
       ⦁ نظام السحوبات
       ⦁ نظام التقييمات والمراجعات
       ⧠ تحسينات واجهة المستخدم
       ⦁ إضافات لوحة الإدارة

       المرحلة الثالثة (المتقدمة):
       ⧠ نظام النزاعات والحلول
       ⦁ مستويات الوكلاء (ذهبي فضي برنز)
       ⦁ خدمات مميزة للوكلاء
       ⧠ تقارير مالية متقدمة
       ⦁ تحليلات أداء الوكلاء
       ⧠ إشعارات تلقائية
       ⦁ إدارة العروض الترويجية

    ي. المتطلبات التقنية
       ⦁ Laravel 9+ مع PHP 8.1+
       ⧠ Laravel Sanctum أو JWT للتوثيق
       ⦁ Livewire أو Vue.js لواجهة المستخدم
       ⧠ Laravel Cashier للمدفوعات
       ⦁ Intervention Image لمعالجة الصور
       ⧠ Laravel Telescope للمراقبة
       ⧠ Laravel Horizon للوظائف المجدولة
       ⧠ Laravel Telescope للمراقبة

    ك. نقاط الأمان الحرجة
       ⦁ جميع العمليات المالية تتم في الـ Backend
       ⦁ عدم الاعتماد على البيانات القادمة من المتصفح
       ⧠ استخدام Hash للبيانات الحساسة
       ⦁ التحقق من الصلاحيات في كل طلب
       ⧠ تسجيل جميع العمليات المهمة
       ⦁ منع هجمات CSRF و XSS
       ⧠ استخدام تشفير SSL/HTTPS
       ⦁ حماية ضد هجمات SQL Injection
       ⧠ التحقق من صرف الملفات المرفوعة
       ⦁ استخدام Laravel Validation Rules
       ⧠ تطبيق Rate Limiting
       ⦁ تدقيق الأمان المنتظم

    ل. اختبارات الجودة
       ⦁ اختبارات الوحدة (PHPUnit)
       ⦁ اختبارات المتكاملة (Feature Tests)
       ⦁ اختبارات الأداء (Performance Tests)
       ⧠ اختبارات الأمان (Security Tests)
       ⦁ اختبارات التوافق (Cross-browser Tests)
       ⧠ اختبارات الجوال (Mobile Tests)
       ⦁ اختبارات التحميل (Load Tests)

6. الدفع والإيداع
   الدفع يجب أن يكون للمتجر فقط، وليس للوكيل.
   طريقة العمل
   .1 العميل يشتري خدمة من صفحة الوكيل.
   .2 يدفع للمتجر.
   .3 النظام يعرف أن الطلب تابع لهذا الوكيل.
   .4 يتم حساب ربح المتجر وربح الوكيل.
   .5 ربح الوكيل يدخل في الرصيد المعلق.
   .6 بعد اكتمال الطلب يتحول إلى رصيد متاح.
   .7 الوكيل يطلب السحب من الأدمن.

7. كيف نعرف أن المبلغ تابع لأي وكيل؟
   كل طلب يجب أن يحفظ هذه البيانات:
   ⦁ رقم الطلب
   ⦁ العميل
   ⦁ الوكيل
   ⦁ الخدمة
   ⦁ رقم عملية الدفع
   ⦁ المبلغ المدفوع
   ⦁ ربح المتجر
   ⦁ ربح الوكيل
   ⦁ حالة الدفع
   ⦁ حالة الطلب
   مثال:
   العميل اشترى خدمة من وكيل اسمه Ahmad.
   النظام يحفظ:
   ⦁ العميل: محمد
   ⦁ الوكيل: Ahmad
   ⦁ الخدمة: تصميم
   ⦁ المبلغ: 100$
⦁	ربح المتجر: 20$
   ⦁ ربح الوكيل: 80$
   بهذا الشكل يظهر في لوحة الأدمن تقرير واضح:
   الطلب العميل الوكيل الخدمة المبلغ ربح المتجر ربح الوكيل

8. نظام الطلبات
   حالات الطلب:
   ⦁ بانتظار الدفع
   ⦁ مدفوع
   ⦁ قيد التنفيذ
   ⦁ تم التسليم
   ⦁ مكتمل
   ⦁ ملغي
   ⦁ مسترجع
   ⦁ نزاع
   التسلسل
   .1 العميل يطلب الخدمة.
   .2 يتم الدفع.
   .3 الطلب يصبح قيد التنفيذ.
   .4 الوكيل يسلم الطلب.
   .5 العميل يؤكد الاستلام.
   .6 الطلب يصبح مكتمل.
   .7 يتم تحويل ربح الوكيل من معلق إلى متاح.
   إذا حصل Refund، يتم خصم ربح الوكيل وعدم السماح بسحبه.

9. نظام التقييم
   التقييم مهم ويجب أن يظهر في صفحة الوكيل.
   من يستطيع التقييم؟
   العميل الذي اشترى الخدمة فقط.
   متى يستطيع التقييم؟
   بعد اكتمال الطلب فقط.
   ماذا يقيّم؟
   ⦁ الخدمة
   ⦁ الوكيل
   بيانات التقييم
   ⦁ عدد النجوم من 1 إلى 5
   ⦁ تعليق
   ⦁ تاريخ التقييم
   ⦁ اسم العميل
   قواعد التقييم
   ⦁ لا يمكن تقييم خدمة بدون شراء.
   ⦁ لا يمكن تقييم الطلب أكثر من مرة.
   ⦁ التقييم يظهر في صفحة الخدمة وصفحة الوكيل.
   ⦁ متوسط تقييم الوكيل يحسب من تقييمات خدماته.

10. الأمان المطلوب
    هذه نقاط ضرورية:
    أ. الصلاحيات
    ⦁ الوكيل لا يدخل لوحة الأدمن.
    ⦁ الوكيل لا يرى طلبات غيره.
    ⦁ الوكيل لا يعدل خدمات غيره.
    ⦁ العميل لا يرى بيانات الوكيل المالية.

ب. حماية الأسعار
مهم جداً:
لا يتم اعتماد السعر القادم من المتصفح.
السعر والعمولة وربح الوكيل يجب أن يتم حسابهم داخل Laravel Backend فقط.

ج. حماية رفع الصور
⦁ السماح فقط بالصور.
⦁ منع ملفات PHP و SVG الخطيرة.
⦁ تحديد حجم الصورة.
⦁ تغيير اسم الملف عند الرفع.

د. حماية الوصف
يجب تنظيف الوصف ومنع:
⦁ الأكواد
⦁ السكربتات
⦁ روابط خارجية
⦁ أرقام التواصل

هـ. تسجيل العمليات
يجب تسجيل العمليات المهمة مثل:
⦁ قبول وكيل
⦁ رفض وكيل
⦁ إضافة خدمة
⦁ قبول خدمة
⦁ تعديل سعر
⦁ طلب سحب
⦁ قبول سحب
⦁ عملية دفع
⦁ استرجاع مبلغ

11. اختبارات للتأكد من أن الشغل صحيح
    بعد انتهاء البرمجة، نختبر التالي:
    طلب الوكالة
    ⦁ المستخدم يستطيع إرسال طلب وكالة.
    ⦁ الطلب يظهر في لوحة الأدمن.
    ⦁ الأدمن يستطيع قبول الطلب.
    ⦁ بعد القبول يصبح المستخدم وكيلاً.
    ⦁ الأدمن يستطيع رفض الطلب مع سبب.

رابط الوكيل
⦁ لكل وكيل رابط خاص.
⦁ لا يمكن تكرار username.
⦁ صفحة الوكيل تعرض خدماته فقط.
⦁ صفحة الوكيل تعرض التقييمات.

الخدمات
⦁ الوكيل يستطيع إضافة خدمة.
⦁ الخدمة لا تظهر قبل موافقة الأدمن.
⦁ الأدمن يستطيع قبول أو رفض الخدمة.
⦁ تعديل الخدمة يعيدها للمراجعة.

إعادة بيع خدمات المتجر
⦁ الوكيل يستطيع اختيار خدمة من المتجر.
⦁ يستطيع إضافة ربح عليها.
⦁ السعر النهائي يظهر صحيحاً.
⦁ لا يمكن التلاعب بالسعر من المتصفح.

الطلبات
⦁ العميل يستطيع شراء خدمة من صفحة الوكيل.
⦁ الطلب يرتبط بالوكيل الصحيح.
⦁ الأدمن يرى العميل والوكيل والخدمة والمبلغ.
⦁ الوكيل لا يرى إلا طلباته.

الأرباح
⦁ بعد الدفع يدخل ربح الوكيل في الرصيد المعلق.
⦁ بعد اكتمال الطلب يتحول إلى رصيد متاح.
⦁ لا يمكن سحب الرصيد المعلق.
⦁ عند الاسترجاع لا يتم احتساب الربح.

السحب
⦁ الوكيل يستطيع طلب سحب.
⦁ لا يمكن طلب سحب أكثر من الرصيد المتاح.
⦁ طلب السحب يظهر للأدمن.
⦁ الأدمن يستطيع قبول أو رفض السحب.

التقييم
⦁ العميل لا يستطيع التقييم بدون شراء.
⦁ التقييم متاح فقط بعد اكتمال الطلب.
⦁ لا يمكن تقييم نفس الطلب مرتين.
⦁ تقييم الخدمة يظهر في صفحة الوكيل.
⦁ متوسط تقييم الوكيل يتم حسابه بشكل صحيح.

الأمان
⦁ الوكيل لا يستطيع فتح طلبات غيره بتغيير الرابط.
⦁ الوكيل لا يستطيع تعديل خدمة غيره.
⦁ لا يمكن رفع ملف غير صورة.
⦁ لا يمكن إدخال كود JavaScript في وصف الخدمة.
⦁ كل عملية مالية لها سجل داخل النظام.

12. الأفضل في التنفيذ
    يفضل تنفيذ النظام على مراحل.
    المرحلة الأولى
    ⦁ طلبات الوكالة
    ⦁ قبول الوكلاء من الأدمن
    ⦁ صفحة الوكيل
    ⦁ إضافة خدمات الوكيل
    ⦁ موافقة الأدمن على الخدمات
    ⦁ الطلبات
    ⦁ حساب الأرباح
    المرحلة الثانية
    ⦁ إعادة بيع خدمات المتجر
    ⦁ السحوبات
    ⦁ التقييمات
    المرحلة الثالثة
    ⦁ النزاعات
    ⦁ مستويات الوكلاء
    ⦁ خدمات مميزة
    ⦁ تقارير متقدمة

الخلاصة
المطلوب هو نظام وكلاء آمن وواضح:
⦁ الوكيل يقدم طلب وكالة.
⦁ الأدمن يقبل أو يرفض.
⦁ كل وكيل له رابط خاص (/agent/username).
⦁ الوكيل يضيف خدمات أو يعيد بيع خدمات المتجر.
⦁ الأدمن يوافق على الخدمات ويحدد نسبة المتجر.
⦁ العميل يدفع للمتجر فقط.
⦁ كل طلب يرتبط بالعميل والوكيل والخدمة.
⦁ أرباح الوكيل تبقى معلقة حتى اكتمال الطلب.
⦁ التقييم يظهر على صفحة الوكيل والخدمة.
⦁ السحب يتم بطلب من الوكيل وموافقة الأدمن.
⦁ جميع المدفوعات تذهب للمتجر وتحسب الأرباح تلقائياً.
⦁ الوكيل يحدد عمولته بنفسه عند إنشاء الخدمة.
⦁ جميع الصفحات متجاوبة مع الجوال.
⦁ الترحيل للقوائم الطويلة لتحسين الأداء.
⦁ نظام أمان متكامل لحماية العمليات المالية.
