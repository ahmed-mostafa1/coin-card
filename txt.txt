
#pull to host

cd ~/laravel-app
git fetch origin

git reset --hard origin/main

git clean -fd \
  -e .env \
  -e storage \
  -e vendor \
  -e public/storage

php artisan optimize:clear

php artisan optimize
php artisan migrate


/home/u434097233/deploy.sh


================================================

Goal: one sentence
Files involved: list paths (or “unknown”)
Current behavior: 1–2 sentences
Expected behavior: 1–2 sentences
Constraints: (Laravel version= 12, DB, auth method)
Relevant snippets: only the minimal code blocks
AI rule: If any of these are missing, proceed with safe assumptions and request only the missing item that blocks correctness.

================================================

you are a senior fullstack laravel developer and your task is to diagnose and fix this bug
